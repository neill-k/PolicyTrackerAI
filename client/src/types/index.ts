// Re-export types from server schema
import type {
  University,
  Policy,
  Source,
  NewUniversity,
  NewPolicy,
  NewSource,
} from "../../../db/schema";

export type { University, Policy, Source, NewUniversity, NewPolicy, NewSource };
